# DTU-forge

This repository holds the minimal installers for DTU compliant Conda environments
using Miniforge.

This will use the `miniforge` constructor machinery as a submodule to track and
update the build-procedure.
